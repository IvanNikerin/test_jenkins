Deploy:
    docker-compose build
    docker-compose up
    
Default port 8080

Speed up in development mode:
	just comment lines 39,40 in webpack.config.js

Proxy/hosts settings:
	server.js contains app that init proxy to tobox and importer backend

	don't forget to change ip's to values that you need