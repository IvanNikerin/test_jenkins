Install dependencies:
	npm install

For development simple run:
	node server

	- and go to http://127.0.0.1:8080/webpack-dev-server/

For build production run:
	webpack

	- this will generate assets folder with all needed bundles